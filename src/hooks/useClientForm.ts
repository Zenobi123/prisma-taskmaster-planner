
// This file is kept to maintain backward compatibility with existing imports
// It re-exports everything from the refactored hooks
import { useClientForm } from "./clientForm";
export * from "./clientForm";
export { useClientForm };
